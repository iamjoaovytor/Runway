# App Identity
APP_IDENTIFIER=com.company.app
SCHEME=App
XCODEPROJ=App.xcodeproj

# Apple Developer
APPLE_ID=your@email.com
TEAM_ID=XXXXXXXXXX
ITC_TEAM_ID=XXXXXXXXXX
FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD=

# Match (Code Signing)
MATCH_GIT_URL=https://github.com/your-org/certificates.git
MATCH_PASSWORD=your-match-encryption-password

# Build
EXPORT_METHOD=app-store
# SKIP_EXPORT=true  # Set to skip IPA export (local builds without provisioning)

# Notifications
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/XXX/XXX

# CI (set automatically in most CI environments)
# CI=true
# CI_KEYCHAIN_PASSWORD=  # Optional: fixed keychain password for CI (random if unset)
